/*
changing a property in the table:
    draw the change

drawing:
    different color to each sprite
    big circle for starting position
    line for each path
*/

play:
    if editor data exists:
        load level data from editor
    else:
        load level data from file
    if debug && editor data doesn't exist:
        clone level data to editor
